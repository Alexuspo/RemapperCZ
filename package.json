{
  "name": "remapper-cz",
  "version": "1.0.0",
  "description": "RemapperCZ documentation site",
  "scripts": {
    "build": "pip install -r requirements.txt && mkdocs build"
  },
  "engines": {
    "node": ">=14.x"
  }
}
